export enum GitLabPullRequestState {
  open = 'opened',
  closed = 'closed',
  merged = 'merged',
  all = 'all',
}

export enum GitLabIssueState {
  open = 'active',
  closed = 'closed',
  all = 'all',
}
